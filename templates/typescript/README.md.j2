# {{ project.name }}

TypeScript MCP scaffold generated by **Unified MCP Studio**.

Install dependencies and build:

```bash
npm install
npm run build
npm start
```

Handlers are emitted under `src/` from your designer definitions.
