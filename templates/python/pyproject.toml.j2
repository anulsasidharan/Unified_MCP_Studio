[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = "Generated MCP server (Unified MCP Studio)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
