# {{ project.name }}

Generated by **Unified MCP Studio** (Python / stdio MCP).

## Run locally

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -e .
python -m generated_mcp.main
```

Replace `generated_mcp` with the package folder name under `src/` if you rename outputs.
