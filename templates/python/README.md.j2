# {{ project.name }} MCP Server

Generated by [Unified MCP Studio](https://github.com/anulsasidharan/unified-mcp-studio).
Runtime: **Python** · Transport: **{{ project.transport }}**

---

## Quick Start

```bash
# 1. Install dependencies
pip install mcp

# 2. Run the server
python main.py
```

---

## Connect to Claude Desktop

Add this block to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{ project.name | lower | replace(' ', '-') }}": {
      "command": "python",
      "args": ["/absolute/path/to/main.py"]
    }
  }
}
```

Config file location:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

---

## Tools ({{ tools | length }})

{% for tool in tools %}
### `{{ tool.name }}`

{{ tool.description or "No description provided." }}

**Input schema:**
```json
{{ tool.input_schema | tojson }}
```

{% endfor %}
{% if not tools %}
No tools defined yet. Add tools in Unified MCP Studio and regenerate.
{% endif %}

---

## Resources ({{ resources | length }})

{% for resource in resources %}
- **{{ resource.name }}** — `{{ resource.uri }}`{% if resource.description %} — {{ resource.description }}{% endif %}

{% endfor %}
{% if not resources %}
No resources defined.
{% endif %}

---

## Prompts ({{ prompts | length }})

{% for prompt in prompts %}
- **{{ prompt.name }}**{% if prompt.description %} — {{ prompt.description }}{% endif %}

{% endfor %}
{% if not prompts %}
No prompts defined.
{% endif %}

---

## Project Structure

```
{{ project.name | lower | replace(' ', '_') }}/
├── main.py           ← Server entry point (stdio transport)
├── tools/
{% for tool in tools %}
│   ├── {{ tool.name }}.py
{% endfor %}
│   └── __init__.py
└── pyproject.toml
```

---

*Generated by Unified MCP Studio — edit in the visual designer and regenerate to update.*
