Prompt to create the docs and modify the rules
----------------------------------------------
 That these are the features which I am trying to build and which I have listed down. Now I don't want you to start the project, so I just want you to change or adopt a rule as per this particular requirement in each and every file that we have inside.cursor/rule folder. Just try to modify that one, not just that. I have a docs folder as well. Inside the docs folder I have an API specification, architecture, DB schema, deployment, and PRD- Just try to build each and everything for me for this particular project, Make sure that you are not starting with the development. So try to add some new rules file if it is required as per my requirements-For Whatsapp, for SMS. I will be using Twilio, so Twilio is my tool which I am going to use-Even for email, I will be using a tool-For deployment, I will be using the GCP  infrastructure. For database, I will be using a Supabase over here. So, frontend-wise I will be using Next.js and typeScript.For backend, I will be using Python and fastAPI. API-wise, I will be using OpenAI API. For speech-to-speech, speech-to-text, text-to-speech, and for embedding and text generation,now, just try to modify all of my rules as per the instruction which I have given to you. Do not start the development.
 
 
 
TASKS creation prompt.

You have access to the entire project directory Based in the markdown files available in the root folder, I want you to modify all the files under @.cursor\rules\ and all files under @docs/ folder. I am going to deploy this application on GCP cloud. Also create TASKS.md file on the root folder. which should contains list of Phases in which the project development goes through and each phases contains sub tasks . each subtasks should be done in seperate feature-branch , give the corresponding feature branch name like "feature/branch-name".  Create this TASKS.md file content so that the dependency issue will be minimized across the development stages. 